[user]
  name = Dan Mundy
  email = drm.mundy@gmail.com
[github]
  user = exchgr
  token = 1532add39e87f4139b9949f1cbe181be
[core]
  excludesfile = ~/.gitignore_global
  quotepath = false
  pager = less -FRX
  editor = $GIT_EDITOR
[color]
  diff = auto
[merge]
  tool = vimdiff
[mergetool]
  prompt = false
[diff]
  tool = vimdiff
[difftool]
  prompt = false
[alias]
  a = add
  an = add -N
  ap = add -p
  b = branch
  ba = branch -a
  bc = rev-parse --abbrev-ref HEAD
  bd = !git branch -D
  c = !git commit -v
  ca = !git commit -av
  cam = !git commit --amend -v
  cl = clone
  cm = !git commit -amv
  co = checkout
  cob = !git checkout -b
  cot = !git checkout --track
  cp = cherry-pick
  d = diff
  dc = diff --cached
  dhh = diff HEAD^ HEAD
  dt = difftool
  dtc = difftool --cached
  f = fetch
  l = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset' --abbrev-commit
  lh = !git log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset' --abbrev-commit | head
  m = merge
  ma = merge --abort
  mt = mergetool
  p = push
  pu = !git push -u origin `git bc`
  r = remote
  ra = remote add
  rb = rebase
  rba = rebase --abort
  rbc = rebase --continue
  rbd = !git push origin --delete
  rbi = rebase -i
  rbs = rebase --skip
  rl = reflog
  rr = remote rm
  rv = remote -v
  s = status
  sa = submodule add
  st = stash
  sta = stash apply
  stc = stash clear
  std = stash drop
  stl = stash list
  stp = stash pop
  sts = stash show
  stsp = stash show -p
  su = submodule update
  sui = submodule update --init
  u = pull
  urb = pull --rebase
[difftool "vimdiff"]
  cmd = $(echo $GIT_EDITOR)diff -R $LOCAL $REMOTE
[mergetool "vimdiff"]
  cmd = $(echo $GIT_EDITOR)diff $MERGED
  keepBackup = false
[web]
  browser = open
[push]
	default = simple
[credential]
	helper = osxkeychain
